function UserCardStateless({ name, age }) {
  return (
    <div>
      <h2>Stateless компонент (картка користувача)</h2>
      <p>Імʼя: {name}</p>
      <p>Вік: {age}</p>
    </div>
  );
}

export default UserCardStateless;